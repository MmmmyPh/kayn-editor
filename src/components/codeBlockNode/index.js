import React from 'react';

const CodeBlockNode = ( props ) => {
	console.log( '==========' );
	console.log( props );
	console.log( '==========' );
	return (
		<div>
			1
		</div>
	);
};

export default CodeBlockNode;